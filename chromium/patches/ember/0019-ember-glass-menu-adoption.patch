diff --git a/chrome/browser/ui/views/ember/ember_glass_overlay_controller.cc b/chrome/browser/ui/views/ember/ember_glass_overlay_controller.cc
index 4f53dff..502aded 100644
--- a/chrome/browser/ui/views/ember/ember_glass_overlay_controller.cc
+++ b/chrome/browser/ui/views/ember/ember_glass_overlay_controller.cc
@@ -434,12 +434,14 @@ EmberGlassOverlayController::~EmberGlassOverlayController() {
 
 void EmberGlassOverlayController::ShowMenu(
     ui::MenuModel* model,
-    const gfx::Point& page_point) {
+    const gfx::Point& page_point,
+    base::OnceClosure menu_closed_callback) {
   CloseCtrlTab(false);
   CloseContextMenu(false);
   if (!model || model->GetItemCount() == 0) {
     return;
   }
+  menu_closed_callback_ = std::move(menu_closed_callback);
   model->MenuWillShow();
   menu_stack_.push_back(model->AsWeakPtr());
   RebuildMenu(model);
@@ -872,6 +874,9 @@ void EmberGlassOverlayController::CloseContextMenu(
   SetVisible(false);
   backdrop_snapshot_.reset();
   material_.Invalidate();
+  if (menu_closed_callback_) {
+    std::move(menu_closed_callback_).Run();
+  }
   if (restore_page_focus && browser_view_->GetActiveWebContents()) {
     browser_view_->GetActiveWebContents()->Focus();
   }
diff --git a/chrome/browser/ui/views/ember/ember_glass_overlay_controller.h b/chrome/browser/ui/views/ember/ember_glass_overlay_controller.h
index 1a552b4..76e2022 100644
--- a/chrome/browser/ui/views/ember/ember_glass_overlay_controller.h
+++ b/chrome/browser/ui/views/ember/ember_glass_overlay_controller.h
@@ -9,6 +9,7 @@
 #include <cstdint>
 #include <vector>
 
+#include "base/functional/callback.h"
 #include "base/memory/raw_ptr.h"
 #include "base/memory/weak_ptr.h"
 #include "chrome/browser/ui/views/ember/ember_glass_material.h"
@@ -53,7 +54,9 @@ class EmberGlassOverlayController : public views::View,
       const EmberGlassOverlayController&) = delete;
   ~EmberGlassOverlayController() override;
 
-  void ShowMenu(ui::MenuModel* model, const gfx::Point& page_point);
+  void ShowMenu(ui::MenuModel* model,
+                const gfx::Point& page_point,
+                base::OnceClosure menu_closed_callback = base::OnceClosure());
   void CancelContextMenu(const ui::MenuModel* model);
   bool IsShowingContextMenu() const;
 
@@ -108,6 +111,7 @@ class EmberGlassOverlayController : public views::View,
   raw_ptr<EmberGlassSwitcherPanel> switcher_panel_ = nullptr;
   EmberGlassMaterial material_;
   std::vector<base::WeakPtr<ui::MenuModel>> menu_stack_;
+  base::OnceClosure menu_closed_callback_;
   std::vector<size_t> visible_model_indices_;
   std::vector<raw_ptr<views::View>> menu_rows_;
   size_t selected_visible_index_ = 0;
diff --git a/chrome/browser/ui/views/extensions/extension_context_menu_controller.cc b/chrome/browser/ui/views/extensions/extension_context_menu_controller.cc
index 369bdf5..c98489d 100644
--- a/chrome/browser/ui/views/extensions/extension_context_menu_controller.cc
+++ b/chrome/browser/ui/views/extensions/extension_context_menu_controller.cc
@@ -9,6 +9,7 @@
 
 #include "chrome/browser/extensions/extension_context_menu_model.h"
 #include "chrome/browser/ui/toolbar/toolbar_action_view_model.h"
+#include "chrome/browser/ui/views/frame/browser_view.h"
 #include "ui/base/l10n/l10n_util.h"
 #include "ui/base/mojom/menu_source_type.mojom-forward.h"
 #include "ui/gfx/paint_vector_icon.h"
@@ -31,7 +32,11 @@ ExtensionContextMenuController::ExtensionContextMenuController(
       observer_(observer),
       context_menu_source_(context_menu_source) {}
 
-ExtensionContextMenuController::~ExtensionContextMenuController() = default;
+ExtensionContextMenuController::~ExtensionContextMenuController() {
+  if (ember_browser_view_ && ember_menu_model_) {
+    ember_browser_view_->CancelEmberContextMenu(ember_menu_model_);
+  }
+}
 
 void ExtensionContextMenuController::ShowContextMenuForViewImpl(
     views::View* source,
@@ -49,6 +54,22 @@ void ExtensionContextMenuController::ShowContextMenuForViewImpl(
       views::MenuRunner::HAS_MNEMONICS | views::MenuRunner::CONTEXT_MENU;
 
   views::Widget* const parent = source->GetWidget();
+  BrowserView* browser_view = parent ? BrowserView::GetBrowserViewForNativeWindow(
+                                           parent->GetNativeWindow())
+                                     : nullptr;
+  if (browser_view) {
+    gfx::Point local_point = point;
+    views::View::ConvertPointFromScreen(browser_view, &local_point);
+    observer_->OnContextMenuShown();
+    // The destructor synchronously cancels this model before `this` can die.
+    browser_view->ShowEmberContextMenu(
+        menu_model, local_point,
+        base::BindOnce(&ExtensionContextMenuController::OnMenuClosed,
+                       base::Unretained(this)));
+    ember_browser_view_ = browser_view;
+    ember_menu_model_ = menu_model;
+    return;
+  }
 
   // Unretained() is safe here as ToolbarActionView will always outlive the
   // menu. Any action that would lead to the deletion of |this| first triggers
@@ -72,10 +93,13 @@ void ExtensionContextMenuController::ShowContextMenuForViewImpl(
 }
 
 bool ExtensionContextMenuController::IsMenuRunning() const {
-  return menu_runner_ && menu_runner_->IsRunning();
+  return (ember_browser_view_ && ember_menu_model_) ||
+         (menu_runner_ && menu_runner_->IsRunning());
 }
 
 void ExtensionContextMenuController::OnMenuClosed() {
+  ember_browser_view_ = nullptr;
+  ember_menu_model_ = nullptr;
   menu_runner_.reset();
   observer_->OnContextMenuClosed();
   menu_adapter_.reset();
diff --git a/chrome/browser/ui/views/extensions/extension_context_menu_controller.h b/chrome/browser/ui/views/extensions/extension_context_menu_controller.h
index d5bed7c..5403f5c 100644
--- a/chrome/browser/ui/views/extensions/extension_context_menu_controller.h
+++ b/chrome/browser/ui/views/extensions/extension_context_menu_controller.h
@@ -13,6 +13,11 @@
 #include "ui/views/context_menu_controller.h"
 
 class ToolbarActionViewModel;
+class BrowserView;
+
+namespace ui {
+class MenuModel;
+}
 
 namespace views {
 class MenuModelAdapter;
@@ -64,6 +69,9 @@ class ExtensionContextMenuController : public views::ContextMenuController {
   // Responsible for running the menu.
   std::unique_ptr<views::MenuRunner> menu_runner_;
 
+  raw_ptr<BrowserView> ember_browser_view_ = nullptr;
+  raw_ptr<ui::MenuModel> ember_menu_model_ = nullptr;
+
   // This controller contains the data for the extension's context menu.
   const raw_ptr<ToolbarActionViewModel> action_model_;
 
diff --git a/chrome/browser/ui/views/frame/browser_view.cc b/chrome/browser/ui/views/frame/browser_view.cc
index f43b4c3..ec9fba7 100644
--- a/chrome/browser/ui/views/frame/browser_view.cc
+++ b/chrome/browser/ui/views/frame/browser_view.cc
@@ -1907,9 +1907,11 @@ BrowserView* BrowserView::GetBrowserViewForBrowser(
 }
 
 void BrowserView::ShowEmberContextMenu(ui::MenuModel* model,
-                                       const gfx::Point& page_point) {
+                                       const gfx::Point& page_point,
+                                       base::OnceClosure menu_closed_callback) {
   if (ember_glass_overlay_) {
-    ember_glass_overlay_->ShowMenu(model, page_point);
+    ember_glass_overlay_->ShowMenu(model, page_point,
+                                   std::move(menu_closed_callback));
   }
 }
 
diff --git a/chrome/browser/ui/views/frame/browser_view.h b/chrome/browser/ui/views/frame/browser_view.h
index 4d612e1..96df550 100644
--- a/chrome/browser/ui/views/frame/browser_view.h
+++ b/chrome/browser/ui/views/frame/browser_view.h
@@ -282,7 +282,9 @@ class BrowserView : public BrowserWindow,
   views::View* contents_container() { return contents_container_; }
 
   void ShowEmberContextMenu(ui::MenuModel* model,
-                            const gfx::Point& page_point);
+                            const gfx::Point& page_point,
+                            base::OnceClosure menu_closed_callback =
+                                base::OnceClosure());
   void CancelEmberContextMenu(const ui::MenuModel* model);
   void ShowEmberCtrlTabSwitcher(bool reverse);
   void CommitEmberCtrlTabSwitcher();
diff --git a/chrome/browser/ui/views/tabs/browser_tab_strip_controller.cc b/chrome/browser/ui/views/tabs/browser_tab_strip_controller.cc
index c1b3e9b..b4b6080 100644
--- a/chrome/browser/ui/views/tabs/browser_tab_strip_controller.cc
+++ b/chrome/browser/ui/views/tabs/browser_tab_strip_controller.cc
@@ -152,6 +152,8 @@ BrowserTabStripController::~BrowserTabStripController() {
   // cancel the menu, otherwise it may try to invoke something on the tabstrip
   // from its destructor.
   if (context_menu_controller_.get()) {
+    browser_view_->CancelEmberContextMenu(
+        context_menu_controller_->GetMenuModel());
     context_menu_controller_.reset();
   }
 
@@ -503,6 +505,11 @@ void BrowserTabStripController::ShowContextMenuForTab(
     return;
   }
 
+  if (context_menu_controller_) {
+    browser_view_->CancelEmberContextMenu(
+        context_menu_controller_->GetMenuModel());
+  }
+
   context_menu_controller_ = std::make_unique<TabContextMenuController>(
       model_->GetTabAtIndex(tab_index.value())->GetHandle(), this);
 
@@ -515,14 +522,18 @@ void BrowserTabStripController::ShowContextMenuForTab(
   context_menu_controller_->LoadModel(
       std::move(model), menu_model_factory_->AsTabMenuModel(model_ptr));
 
-  context_menu_controller_->RunMenuAt(p, source_type, tabstrip_->GetWidget());
+  gfx::Point local_point = p;
+  views::View::ConvertPointFromScreen(browser_view_, &local_point);
+  browser_view_->ShowEmberContextMenu(
+      context_menu_controller_->GetMenuModel(), local_point);
   base::UmaHistogramEnumeration("TabStrip.Tab.Views.ActivationAction",
                                 TabActivationTypes::kContextMenu);
 }
 
 void BrowserTabStripController::CloseContextMenuForTesting() {
   if (context_menu_controller_) {
-    context_menu_controller_->CloseMenu();
+    browser_view_->CancelEmberContextMenu(
+        context_menu_controller_->GetMenuModel());
   }
 }
 
diff --git a/chrome/browser/ui/views/tabs/new_tab_button.cc b/chrome/browser/ui/views/tabs/new_tab_button.cc
index 497a1db..aabdda2 100644
--- a/chrome/browser/ui/views/tabs/new_tab_button.cc
+++ b/chrome/browser/ui/views/tabs/new_tab_button.cc
@@ -9,6 +9,7 @@
 
 #include "base/metrics/user_metrics.h"
 #include "chrome/app/chrome_command_ids.h"
+#include "chrome/browser/ui/browser.h"
 #include "chrome/browser/ui/browser_commands.h"
 #include "chrome/browser/ui/browser_element_identifiers.h"
 #include "chrome/browser/ui/browser_window/public/browser_window_features.h"
@@ -17,6 +18,7 @@
 #include "chrome/browser/ui/tabs/tab_strip_model.h"
 #include "chrome/browser/ui/ui_features.h"
 #include "chrome/browser/ui/views/tabs/tab_strip.h"
+#include "chrome/browser/ui/views/frame/browser_view.h"
 #include "chrome/grit/generated_resources.h"
 #include "components/tabs/public/tab_group.h"
 #include "ui/base/interaction/element_identifier.h"
@@ -45,14 +47,34 @@ NewTabButton::NewTabButton(PressedCallback callback,
   SetProperty(views::kElementIdentifierKey, kNewTabButtonElementId);
 }
 
-NewTabButton::~NewTabButton() = default;
+NewTabButton::~NewTabButton() {
+  Browser* browser = browser_ ? browser_->GetBrowserForMigrationOnly() : nullptr;
+  BrowserView* browser_view =
+      browser ? BrowserView::GetBrowserViewForBrowser(browser) : nullptr;
+  if (browser_view && context_menu_model_) {
+    browser_view->CancelEmberContextMenu(context_menu_model_.get());
+  }
+}
 
 void NewTabButton::ShowContextMenuForViewImpl(
     View* source,
     const gfx::Point& point,
     ui::mojom::MenuSourceType source_type) {
   if (features::IsTabGroupMenuMoreEntryPointsEnabled()) {
+    Browser* browser = browser_->GetBrowserForMigrationOnly();
+    BrowserView* browser_view =
+        browser ? BrowserView::GetBrowserViewForBrowser(browser) : nullptr;
+    if (browser_view && context_menu_model_) {
+      browser_view->CancelEmberContextMenu(context_menu_model_.get());
+    }
     context_menu_model_ = std::make_unique<NewTabButtonMenuModel>(browser_);
+    if (browser_view) {
+      gfx::Point local_point = point;
+      views::View::ConvertPointFromScreen(browser_view, &local_point);
+      browser_view->ShowEmberContextMenu(context_menu_model_.get(),
+                                         local_point);
+      return;
+    }
 
     int32_t menu_runner_flags =
         views::MenuRunner::HAS_MNEMONICS | views::MenuRunner::CONTEXT_MENU;
